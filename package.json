{
  "name": "typer",
  "version": "1.0.0",
  "description": "A game to see how fast you can type",
  "main": "index.js",
  "scripts": {
    "dev": "npm run clean && rimraf parcel src/index.pug",
    "build": "npm run clean && parcel build src/index.pug --out-dir docs --public-url /typer",
    "clean": "rimraf ./docs && rimraf ./dist && rimraf ./.cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "pug": "^2.0.4",
    "rimraf": "^3.0.2",
    "sass": "^1.26.5"
  }
}
